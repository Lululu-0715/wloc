# WLOC 定位修改 — 部署到自己的仓库

本包已把所有地址改好，指向你的仓库 `Lululu-0715/wloc`。
脚本文件用的是可用版本，MD5 校验一致，**不需要做任何修改，原样上传即可**。

---

## 第一步：上传文件到 GitHub

在你的仓库 `Lululu-0715/wloc` 里，按下面的结构放好。
（GitHub 网页版：进入对应目录 → **Add file** → **Upload files** → 选择文件 → **Commit changes**）

| 上传到哪个目录 | 用本包里的哪个文件 | 说明 |
|---|---|---|
| `dist/` | `dist/wloc.js` | 核心脚本，改写坐标 |
| `dist/` | `dist/wloc-settings.js` | 接收选点页面的保存请求 |
| `modules/` | `modules/wloc.module` | **小火箭用这个** |
| `modules/` | `modules/wloc.sgmodule` | Surge / Egern |
| `modules/` | `modules/wloc.conf` | Quantumult X |
| `modules/` | `modules/wloc.lpx` | Loon |
| `modules/` | `modules/wloc.stoverride` | Stash |
| `worker/` | `worker/wloc-worker.js` | 自建选点页面（可选） |
| `worker/` | `worker/wrangler.jsonc` | Worker 部署配置 |
| `worker/dist/` | `worker/dist/_routes.json` | Pages 路由配置 |
| `docs/` | `docs/shortcut-guide.md` | 使用说明文档 |
| 根目录 | `wloc.jpg` | 模块图标 |
| 根目录 | `README.md` | 仓库说明 |
| 根目录 | `.gitignore` | Git 忽略规则 |

> **只关心小火箭的话，`dist/` + `modules/` 这 7 个文件是必须的**，`worker/`、`docs/` 是选点页面相关，不用自建选点页面可以不传。
> 若 `dist/` 里已有同名文件，上传时会覆盖，直接确认提交即可。

**手机上传小技巧：** GitHub 手机网页不支持拖拽。用「Upload files」按钮从「文件」App 里选文件，一次可以多选。

---

## 第二步：验证上传成功

浏览器打开这三个链接，能看到内容（不是 404）就没问题：

- https://raw.githubusercontent.com/Lululu-0715/wloc/refs/heads/main/dist/wloc.js
- https://raw.githubusercontent.com/Lululu-0715/wloc/refs/heads/main/dist/wloc-settings.js
- https://raw.githubusercontent.com/Lululu-0715/wloc/refs/heads/main/modules/wloc.module

---

## 第三步：小火箭订阅

**订阅地址：**
```
https://raw.githubusercontent.com/Lululu-0715/wloc/refs/heads/main/modules/wloc.module
```

操作：小火箭 → 配置 → 模块 → 左滑删除旧模块 → 右上角 `+` → 粘贴上面地址。

> ⚠️ 小火箭用 `wloc.module`，**不要**用 `.sgmodule`。

---

## 第四步：检查关键开关（不生效基本都是这里）

1. **HTTPS 解密**开关 → 打开
2. **域名列表** → 确认包含这 5 个：
   ```
   gs-loc.apple.com
   gs-loc-cn.apple.com
   gsp-ssl.ls.apple.com
   bluedot.is.autonavi.com
   bluedot.is.autonavi.com.gds.alibabadns.com
   ```
3. **证书** → 已安装并信任（设置 → 通用 → 关于本机 → 证书信任设置 → 打开开关）

> `gsp-ssl.ls.apple.com` 是 Apple 新版定位接口，**缺了它在新系统上不生效** —— 这也是之前失败的根本原因。

---

## 第五步：使用

1. 打开选点页面：https://wloc.333012.xyz/ （或你自己部署的 Worker 地址）
2. 选好位置 → 点「储存到设备」
3. 打开地图 App 验证

> Safari 的保存请求必须经过代理客户端才能被拦截，所以小火箭要保持开启。

---

## （可选）自建选点页面

想用自己的域名，可以把 `worker/wloc-worker.js` 部署到 Cloudflare Workers：

**命令行：**
```bash
cd worker
npx wrangler deploy
```

**网页：** Cloudflare Dashboard → Workers & Pages → 创建 Worker → 整段粘贴 `wloc-worker.js` → 部署。

纯静态页面，**不需要 KV、数据库或环境变量**。部署后把页面里的选点地址换成你的 Worker 域名即可。

> `wrangler.pages.jsonc.disabled` 是 Pages 专用配置，用 Workers 部署时保持禁用（已重命名）。

---

## 恢复真实定位

- 关闭模块，或
- 选点页面点「清除数据」，或
- 客户端里清除持久化数据（键名 `wloc_settings`）

iOS 26+ 有系统级定位缓存，可能需要重启设备。

---

## 常见问题

**Q：显示"需要代理模块支持"？**
A：说明请求没被拦截。依次检查：模块是否启用 → HTTPS 解密是否打开 → 证书是否信任 → 域名列表是否含全部 5 个。

**Q：选点后地图没变化？**
A：① 确认选点页面用的是 https；② 保存后彻底杀掉地图 App 再重开；③ iOS 新版系统试着重启设备。

**Q：模块订阅显示 404？**
A：GitHub raw 有缓存，等 1-2 分钟重试；或确认文件确实已提交到 `main` 分支。
