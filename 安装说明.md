# 安装说明

仓库地址已全部改好指向 `Lululu-0715/wloc`，脚本与可用版一致，**原样上传即可，不用改任何东西**。

---

## 1. 上传

进仓库 → **Add file** → **Upload files** → 选文件 → **Commit changes**。

| 目录 | 文件 |
|---|---|
| `dist/` | `wloc.js`、`wloc-settings.js` |
| `modules/` | `wloc.module`（小火箭）、`wloc.sgmodule`、`wloc.conf`、`wloc.lpx`、`wloc.stoverride` |
| `worker/` | `wloc-worker.js`、`wrangler.jsonc`、`.gitignore`、`dist/_routes.json` |
| `docs/` | `shortcut-guide.md` |
| 根目录 | `wloc.jpg`、`README.md`、`.gitignore` |

> 只玩小火箭 → `dist/` + `modules/` 共 **7 个文件**就够。
> 同名文件会覆盖，直接提交。
> GitHub 手机网页不支持拖拽，用「Upload files」从「文件」App 选。

---

## 2. 订阅

```
https://raw.githubusercontent.com/Lululu-0715/wloc/refs/heads/main/modules/wloc.module
```

小火箭 → 配置 → 模块 → **左滑删掉旧模块** → 右上角 `+` → 粘贴地址。

> 用 `.module`，**不是** `.sgmodule`。

---

## 3. 检查开关（不生效基本都是这）

- **HTTPS 解密** → 打开
- **证书** → 装两遍：客户端装一次，再去 `设置 → 通用 → 关于本机 → 证书信任设置` 打开开关
- **域名列表** → 必须这 5 个，缺一不可：

```
gs-loc.apple.com
gs-loc-cn.apple.com
gsp-ssl.ls.apple.com
bluedot.is.autonavi.com
bluedot.is.autonavi.com.gds.alibabadns.com
```

> 最常缺的是 `gsp-ssl.ls.apple.com`（Apple 新版定位接口），缺了在新系统上完全不生效。

---

## 4. 用

1. 打开选点页面 **https://wloc-1993.575613136.workers.dev**
2. 选位置 → 点「储存到设备」
3. 打开地图 App 看

代理要保持开着，否则 Safari 的保存请求拦不到。

---

## 5. iOS 26+ 要重启设备

系统会缓存旧坐标 —— 脚本已生效、日志显示改了，地图却还是老位置。

**只能重启。** 飞行模式、关定位都不管用。

恢复真实定位也一样，清完数据要重启。

---

## 恢复真实定位

选点页面点**清除数据**，或关模块，然后重启。

> 改过模块参数的，光清数据不够，参数也要改回默认值 `113.94114 / 22.544577`。

---

## 排查

| 症状 | 原因 |
|---|---|
| 订阅 404 | raw 缓存，等 1~2 分钟 |
| 提示"需要代理模块支持" | 模块开关 / MITM / 证书 / 5 个域名，挨个查 |
| 提示成功但地图没变 | 重启设备 |

---

## 自建选点页面（可选）

`worker/wloc-worker.js` 整段粘到 Cloudflare Workers 网页控制台 → 部署。纯静态，不要数据库。

命令行：`cd worker && npx wrangler deploy`

> `wrangler.pages.jsonc.disabled` 用 Workers 部署时保持禁用，别改回 `.jsonc`。
